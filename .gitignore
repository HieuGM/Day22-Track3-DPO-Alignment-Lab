# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
# NOTE: notebooks/*.ipynb IS committed for submission (with output cells preserved
# for grading). The Jupytext `.py` files are the source-of-truth template;
# `.ipynb` is the student's executed deliverable.

# Lab artifacts. Keep the small grading evidence in Git for code-only
# Submission Option C; omit model weights and checkpoint directories.
data/pref/*
!data/pref/train.parquet
!data/pref/eval.parquet
data/eval_prompts.json
adapters/sft-mini/*
!adapters/sft-mini/adapter_config.json
!adapters/sft-mini/sft_metrics.json
adapters/dpo/*
!adapters/dpo/adapter_config.json
!adapters/dpo/dpo_metrics.json
!adapters/dpo/trainer_log_history.json
adapters/orpo/
gguf/
*.gguf
runs/
wandb/

# HuggingFace cache (Colab + local — model weights are huge)
.cache/
hf_cache/

# Env / secrets
.env
.env.local
*.token

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
